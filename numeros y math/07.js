// Orden de las operaciones

let resultado;

resultado = (20 + 30) * 2;
resultado = (100 + 200 + 300) * .2; // Ejemplo de sacar el 20% de descuento
resultado = (100 + 500) * 1.31 // Ejemplo de agregarle impuestos por ejemplo

// console.log(resultado);

// Incrementos

let puntaje = 10;

// Incremento en  1 ++
puntaje++; // 10
++puntaje; // 12

// Decrecer en 1 --
puntaje--; // 12
--puntaje; // 10

// incrementar o decrementar en varios

puntaje += 10;
puntaje -= 10;


console.log(puntaje);